        // setup for time chart of volumes 
	var years = ["2006", "2007",  "2008", "2009", "2010", "2011"]; // TODO: generate this in erb 
	var data = [16, 12, 60,71,186,101]; // TODO: generate this part in erb 
	var w = 180, h = 100, p = 20;

        function startAll(){
                var title = "ГРОШІ, ОТРИМАНІ ЧЕРЕЗ ЗАКУПІВЛІ"; 
		timeChart(data, years, w, h, p, title);
                barChart(data, w, h, p);
        }
